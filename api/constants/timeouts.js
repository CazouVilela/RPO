/**
 * Constantes de Timeouts e Intervals - RPO V5
 */

const TIMEOUTS = {
  CONNECTION: 2000,
  SHORT_OPERATION: 5000,
  DEFAULT_API: 10000,
  SETUP_OPERATION: 15000,
  LONG_OPERATION: 30000,
  ONE_MINUTE: 60000,
  TWO_MINUTES: 120000,
  TEN_MINUTES: 600000,
  IDLE_CONNECTION: 30000,
  RETRY_MAX_DELAY: 2000,
  RETRY_DELAY: 500,
  LOCK: 1000
};

const INTERVALS = {
  SYNC: 1000,
  SCHEMA_DISCOVERY: 60000,
  FALLBACK_CHECK: 10000
};

const TTL = {
  THIRTY_DAYS: 30 * 24 * 60 * 60,
  ONE_DAY: 24 * 60 * 60,
  ONE_HOUR: 60 * 60,
  TEN_MINUTES: 10 * 60,
  FIVE_MINUTES: 5 * 60
};

module.exports = {
  TIMEOUTS,
  INTERVALS,
  TTL
};
